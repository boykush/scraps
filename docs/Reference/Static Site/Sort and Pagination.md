#[[Emit/Static Site]]

The wiki index page is sorted by `committed_date` (default) or
`linked_count`, and optionally paginated. Configure `sort_key` and
`paginate_by` under `[ssg]` in [[Reference/Configuration]].
