#[[Emit/Static Site]]

Each `#[[tag]]` referenced by at least one scrap gets a generated index
page listing all scraps that reference it. Nested tags (`#[[a/b/c]]`)
auto-aggregate to their parents — see [[Reference/Wiki-link/Nested Tag]].
